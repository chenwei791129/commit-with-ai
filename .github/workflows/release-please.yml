name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        # Configuration is read from release-please-config.json
        # This will:
        # 1. Create/update release PR
        # 2. Create GitHub Release and tag when PR is merged
        # 3. Trigger publish-pypi workflow via release event
